# ============================================================
# XPLAIN .gitignore  (safe for src/ layout)
# ============================================================

# ------------------------------------------------------------
# 1) Environment / secrets (never commit)
# ------------------------------------------------------------
.env
.env.*
!.env.example
!.env.sample
.env.yaml
.env.yml

# ------------------------------------------------------------
# 2) Python cache / compiled files
# ------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class

# ------------------------------------------------------------
# 3) Virtual environments
# ------------------------------------------------------------
.venv/
venv/
env/
ENV/
.python-version

# ------------------------------------------------------------
# 4) Build / packaging artifacts
# ------------------------------------------------------------
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/
*.whl

# ------------------------------------------------------------
# 5) Jupyter / notebooks artifacts
# ------------------------------------------------------------
.ipynb_checkpoints/
*.nbconvert.ipynb

# ------------------------------------------------------------
# 6) OS / editor junk
# ------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ------------------------------------------------------------
# 7) Logs
# ------------------------------------------------------------
*.log
logs/
log/

# ------------------------------------------------------------
# 8) Test / coverage artifacts
# ------------------------------------------------------------
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.cache/

# ------------------------------------------------------------
# 9) DATA + MODEL WEIGHTS (ROOT ONLY)
#
# IMPORTANT:
# - Leading "/" means "only at repo root"
# - So src/xplain_package/models/ (code) is NOT ignored.
# ------------------------------------------------------------

# Ignore ONLY the repo-root models/ folder contents (weights)
#/models is tracked via .gitkeep
/models/*
!/models/.gitkeep

# Optional: ignore ONLY repo-root raw_data if you want
# (commented out so you can still version small samples)
# /raw_data/*

# Optional: ignore big local datasets at repo root
# /data/*
# /datasets/*
# /downloads/*

# ------------------------------------------------------------
# 10) Docker leftovers (optional)
# ------------------------------------------------------------
*.tar
*.tgz

# ------------------------------------------------------------
# 11) Misc
# ------------------------------------------------------------
tmp/
temp/
